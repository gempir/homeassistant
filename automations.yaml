# Motion
- alias: "Kitchen Light on Motion"
  initial_state: true
  trigger:
    - platform: state
      entity_id: binary_sensor.kitchen_motion
      to: "on"
  action:
    - service: light.turn_on
      entity_id: light.kitchen_ceiling_light

- alias: "Kitchen Light off after 1 Minute"
  initial_state: true
  trigger:
    - platform: state
      entity_id: binary_sensor.kitchen_motion
      to: "off"
      for:
        minutes: 1
  action:
    service: light.turn_off
    entity_id: light.kitchen_ceiling_light

- alias: "Entry Light on Motion"
  initial_state: true
  trigger:
    - platform: state
      entity_id: binary_sensor.entryway_motion
      to: "on"
  action:
    - service: light.turn_on
      entity_id: light.entryway_ceiling_light

- alias: "Entry Light off after 1 Minute"
  initial_state: true
  trigger:
    - platform: state
      entity_id: binary_sensor.entryway_motion
      to: "off"
      for:
        minutes: 1
  action:
    service: light.turn_off
    entity_id: light.entryway_ceiling_light

- alias: "Bathroom Light on Motion"
  initial_state: true
  trigger:
    - platform: state
      entity_id: binary_sensor.bathroom_motion
      to: "on"
  action:
    - service: light.turn_on
      entity_id: light.bathroom_ceiling_light

- alias: "Bathroom Light off after 1 Minute"
  initial_state: true
  trigger:
    - platform: state
      entity_id: binary_sensor.bathroom_motion
      to: "off"
      for:
        minutes: 1
  action:
    service: light.turn_off
    entity_id: light.bathroom_ceiling_light

- alias: "Toilet Light on Motion"
  initial_state: true
  trigger:
    - platform: state
      entity_id: binary_sensor.toilet_motion
      to: "on"
  action:
    - service: light.turn_on
      entity_id: light.toilet_ceiling_light

- alias: "Toilet Light off after 1 Minute"
  initial_state: true
  trigger:
    - platform: state
      entity_id: binary_sensor.toilet_motion
      to: "off"
      for:
        minutes: 1
  action:
    service: light.turn_off
    entity_id: light.toilet_ceiling_light

- alias: "Living Room Switch Left"
  initial_state: true
  trigger:
    platform: event
    event_type: deconz_event
    event_data:
      id: living_room_switch
      event: 2002
  action:
    service: light.toggle
    entity_id: light.couch_light

- alias: "Living Room Switch Right"
  initial_state: true
  trigger:
    platform: event
    event_type: deconz_event
    event_data:
      id: living_room_switch
      event: 1002
  action:
    service: light.toggle
    entity_id: light.living_room_ceiling_light

- alias: "Office Switch Left"
  initial_state: true
  trigger:
    platform: event
    event_type: deconz_event
    event_data:
      id: office_switch
      event: 2002
  action:
    service: light.toggle
    entity_id: light.desk_light

- alias: "Office Switch Right"
  initial_state: true
  trigger:
    platform: event
    event_type: deconz_event
    event_data:
      id: office_switch
      event: 1002
  action:
    service: light.toggle
    entity_id: light.office_ceiling_light

- alias: "Bedroom Switch Left"
  initial_state: true
  trigger:
    platform: event
    event_type: deconz_event
    event_data:
      id: bedroom_switch
      event: 2002
  action:
    service: light.toggle
    entity_id: light.bed_light

- alias: "Bedroom Switch Right"
  initial_state: true
  trigger:
    platform: event
    event_type: deconz_event
    event_data:
      id: bedroom_switch
      event: 1002
  action:
    service: light.toggle
    entity_id: light.bedroom_ceiling_light
